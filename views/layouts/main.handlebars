<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Eat da Burgers</title>
        {{#each ../public}}
            <link rel="stylesheet" href="/public/{{this}}">
        {{/each}}
    </head>
    <body>
        {{{ body }}}
    </body>
</html>